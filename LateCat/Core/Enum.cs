namespace LateCat.Core
{
    public enum ButtonState : byte
    {
        Normal,
        FrontEnd,
        BackEnd,
        Disable
    }

    public enum VerifyState : byte
    {
        None,
        Succeed,
        Failed
    }
}
